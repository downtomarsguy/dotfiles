(deflisten notifs :initial ""
  `python ~/.config/eww/notifications/main.py`)

(defwindow themes
           :monitor 0
           :geometry (geometry :x "20px"
                               :y "20px"
                               :anchor "top right")
           :stacking "overlay"
           (literal :content notifs))
